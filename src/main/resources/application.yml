# =================================================================
# PROFILES: Local
# ==================================================================
server:
  port: 8080
  servlet:
    context-path: /
    session:
      timeout: 3000m # ?????? ==1800s

spring:
  config.activate.on-profile: local
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/book_mall?characterEncoding=UTF-8&serverTimezone=UTC
    username: bookDb
    password: tkdlemqnr@1

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true # ???? sql
    properties:
      hibernate:
        format_sql: true # ??? sql format ??
        default_batch_fetch_size: 500 # ??? ??? ??? in ??? ??? ?? ???
    open-in-view: false # ??? ????? ?? ??? ???? ??? ??

  servlet:
    multipart:
      max-file-size: 10MB # ?? ??? ??? ??? ????? 10MB? ??
      max-request-size: 100MB # ?? ??? ?? ? ?? ?? ???? 100MB? ??


logging:
  level:
    org.hibernate.type: trace #???? ??? ????? ? ? ?? ?? ??